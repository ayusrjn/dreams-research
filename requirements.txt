# Phase 1: Data Pull & Freezing
requests>=2.28.0

# Phase 2: Feature Extraction (install when ready)
# torch>=2.0.0
# clip @ git+https://github.com/openai/CLIP.git
# sentence-transformers>=2.2.0
# transformers>=4.30.0
